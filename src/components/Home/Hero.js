import React from "react";
import Card from "components/Card";

const Hero = () => (
  <section className="hero">
    <h1>Welcome to Animo City</h1>
    <button>Explore Map</button>
  </section>
);

export default Hero;
